<%= form_with(model: Room.new, remote: true, class: "d-flex" ) do |f| %>
  <%= f.text_field :name, class: "form-control", placeholder: "Enter room name", autocomplete: 'off' %>
  <%= f.submit "Create Room", class: "btn btn-primary" %>
<% end %>
